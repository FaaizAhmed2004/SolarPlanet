# SMTP Configuration for Nodemailer - Namecheap Private Email
SMTP_HOST=mail.privateemail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@theenergyplanet.com
SMTP_PASS=your-email-password

# Email Addresses
BUSINESS_EMAIL=info@theenergyplanet.com
FROM_EMAIL=info@theenergyplanet.com
REPLY_TO_EMAIL=info@theenergyplanet.com

# Namecheap Private Email Configuration:
# 1. Use mail.privateemail.com as SMTP host
# 2. Port 587 with STARTTLS (SMTP_SECURE=false)
# 3. Use your full email address as SMTP_USER
# 4. Use your email password as SMTP_PASS

# Alternative Namecheap SMTP settings:
# - SMTP_HOST: mail.privateemail.com
# - SMTP_PORT: 465 (with SMTP_SECURE=true for SSL)
# - SMTP_PORT: 587 (with SMTP_SECURE=false for STARTTLS)